{
  "name": "auto-pipeline",
  "description": "Agent pipeline rules, scripts, agents, skills and a startup quiz gate for any repo",
  "version": "0.4.2",
  "userConfig": {
    "runtime": {
      "type": "string",
      "title": "Runtime",
      "description": "How this machine runs agents. auto works it out, orca uses Orca terminals, plain and cloud use subagents only.",
      "options": ["auto", "orca", "plain", "cloud"],
      "default": "auto"
    },
    "language": {
      "type": "string",
      "title": "Language",
      "description": "Language to talk to you in. Code, file names and rule text stay English.",
      "default": "en"
    },
    "max_agents": {
      "type": "number",
      "title": "Max agents",
      "description": "How many agents may run at once on this machine.",
      "min": 1,
      "max": 16,
      "default": 4
    }
  }
}
